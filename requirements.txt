torch>=2.0.0
torchvision>=0.15.0
transformers>=4.36.0
accelerate>=0.25.0
sentence-transformers>=2.2.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
einops>=0.7.0
tqdm>=4.65.0
pyyaml>=6.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Cryptographic and hashing dependencies
xxhash==3.5.0  # Fast non-cryptographic hash for challenge IDs and performance-critical paths
web3>=6.0.0  # Blockchain integration for Web3BlockchainClient (optional)

# Fuzzy hashing support (optional)
# ssdeep requires libfuzzy system library; install separately if needed
py-tlsh==4.7.2

pytest>=7.0.0

# Topographical learning dependencies
umap-learn>=0.5.0  # UMAP dimensionality reduction
minisom>=2.3.0  # Self-Organizing Maps
plotly>=5.0.0  # Interactive visualizations
# Note: scikit-learn already included above for t-SNE and other methods

